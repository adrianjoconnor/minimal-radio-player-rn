class Station {
  constructor(id, url, title, genre, artwork) {
    this.id = id;
    this.url = url;
    this.title = title;
    this.genre = genre;
    this.artwork = artwork;
  }
}

export default Station;
